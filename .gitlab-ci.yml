test:
  cache:
    paths:
      - venv/
  script:
  - virtualenv -q venv
  - source venv/bin/activate
  - export http_proxy=http://127.0.0.1:6666
  - export https_proxy=http://127.0.0.1:6666
  - pip install -e .
  - pip install nose
  - nosetests test/unit